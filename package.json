{
  "name": "trang-chu-react-hooks",
  "version": "1.0.0",
  "private": true,
  "description": "A React application using hooks to manage state and lifecycle.",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",  // Runs the app in development mode
    "build": "react-scripts build",  // Bundles the app into static files for production
    "test": "react-scripts test",    // Starts the test runner
    "eject": "react-scripts eject"   // Removes the single build dependency from your project
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}